# Multi-Agent AI Research Assistant

A local-first, multi-agent AI research assistant with LangGraph-style workflows, RAG, Skills pluginization, and MCP context.

## Quickstart

### Option A: uv (recommended)

1. Create a virtual environment: `uv venv`
2. Install dependencies: `uv pip install -r requirements.txt`
3. Run: `uv run python -m app.main`

### Option B: pip

1. Create a virtual environment.
2. Install dependencies: `pip install -r requirements.txt`
3. Run: `python -m app.main`

## Roadmap

- LangGraph integration
- RAG retrieval + vector store
- Skills plugin loader
- MCP context routing
- Web dashboard
